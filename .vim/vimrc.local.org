" vim-quickrun {{{
"let g:KazuakiMDatabase = [
"    \ '--user=xxxx --password=xxxx --host=xxxx --port=3306 --database=database --connect-timeout=20']
"let g:quickrun_config['sql/mysql']['cmdopt'] = g:KazuakiMDatabase[0]
"}}}

"tags {{{
function! s:setTags(name, other) abort "{{{
    if match(expand('%:p:h'), 'tests') > 0
        let &l:tags = $HOME . '/.vim/tags/' . a:name . 'Test.tags' . a:other
    else
        let &l:tags = $HOME . '/.vim/tags/' . a:name . '.tags' . a:other
    endif
endfunction "}}}

"autocmd MyAutoCmd BufNewFile,BufRead $HOME/sample/*.php          call s:setTags('sample', ',' . $HOME . '/.vim/tags/global.tags')
"autocmd MyAutoCmd BufNewFile,BufRead $HOME/huga/*.{js,json,html} setlocal tags=$HOME/.vim/tags/huga.tags
"autocmd MyAutoCmd BufNewFile,BufRead C:/usr/local/src/hoge/*.php setlocal tags=$HOME/.vim/tags/hoge.tags
"}}}

"vim-ms-translator {{{
" @url https://portal.azure.com
"let g:mstranslator#Config.subscription_key = 'check Google Drive'
"}}}
