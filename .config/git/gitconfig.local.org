[push]
    default = simple
[core]
    autocrlf = input
[alias]
    #df = diff --ignore-space-at-eol --ignore-space-change
    # from 1.8.4
    df  = diff --ignore-space-at-eol --ignore-blank-lines --ignore-all-space
# vim:ft=gitconfig
