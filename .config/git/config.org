[include]
    # from 1.7.10
    path = ~/.config/git/gitconfig.local
# from 2.13.0   https://git-scm.com/docs/git-config#_conditional_includes
#[includeIf "gitdir:/path/to/foo/.git"]
#   path = ~/company/setting
[alias]
    dff = difftool
    lg  = log --abbrev-commit --date=relative --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold cyan)<%an>%Creset'
    llg = !git log --abbrev-commit --date=relative --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold cyan)<%an>%Creset' origin/master..`git rev-parse --abbrev-ref HEAD`
    rbi = !git rebase -i $(git merge-base master HEAD)
    st  = status
    #MEMO:http://pocke.hatenablog.com/entry/2017/06/10/214322
    #fpr = "!f(){ git fetch origin pull/$1/head:$1 && git checkout $1; };f"
    #up  = "!{ git pull && git status;};"
[branch]
    autosetuprebase = always
[core]
    editor            = vim
    precomposeunicode = true
    excludesfile      =  ~/.config/git/gitignore_global
[color]
    ui = auto
[diff]
    algorithm           = histogram
    compactionHeuristic = true
    tool                = vimdiff
[difftool "sourcetree"]
    cmd  = opendiff \"$LOCAL\" \"$REMOTE\"
    path =
[merge]
    # from 1.7.6
    ff   = true
    tool = vimdiff
[merge "ours"]
    driver = true
[mergetool]
    keepBackup = true
[mergetool "sourcetree"]
    cmd           = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
    trustExitCode = true
[pull]
    # from 1.7.9
    rebase = true
[pager]
    diff = diff-highlight | less
    log  = diff-highlight | less
    show = diff-highlight | less
[rerere]
    enabled = true
# vim:ft=gitconfig
